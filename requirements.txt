# Find Evil! — required downloads manifest
# =============================================================================
# This file has two halves:
#   1. pip-installable host DFIR Python tooling (the uncommented lines) — run:
#         pip install -r requirements.txt      (or: uv pip install -r requirements.txt)
#   2. Large external artifacts that are NOT pip-installable (the comments) —
#      fetch by hand from the links. The SANS SIFT Workstation OVA is the big one.
#
# Full human-readable table: README.md -> "Required downloads & install files".
# NOTE: the MCP services (services/mcp Rust, services/agent* Python) build via
# scripts/install.sh and manage their own deps with uv per-service — they are
# NOT installed from this file.

# --- pip-installable host DFIR tooling (Local-host mode / Path B) -------------
# Pinned to match the SIFT Workstation 2026.03.24 build (its volatility3.sls
# installs latest-at-build-time; 2.27.0 was current on 2026-03-24, before 2.28.0).
volatility3==2.28.0          # memory forensics; powers vol_pslist / psscan / psxview / malfind
matplotlib                   # report figures in scripts/render_report.py (host-side; local + --sift)

# --- REQUIRED large download: SANS SIFT Workstation OVA (Path A) --------------
# SANS SIFT Workstation OVA    ~9.3 GB    https://www.sans.org/tools/sift-workstation/
#   -> save as  sift-2026.03.24.ova  in the repo root (gitignored via *.ova; never committed).
#   -> bundles the AGPL/GPL DFIR binaries (Volatility3, Hayabusa, Chainsaw,
#      Velociraptor, YARA), so SIFT-VM mode needs NONE of the host binaries below.
#   -> consumed by scripts/find-evil-sift, scripts/sift-vm-bootstrap.sh, and the
#      Packer L3 build (packer/sift-microvm.pkr.hcl).
# VMware Workstation                      https://www.vmware.com/products/desktop-hypervisor.html
#   -> provides vmrun + ovftool for sift-vm-bootstrap.sh (VMware-only today).

# --- host DFIR binaries (Local-host mode / Path B; skip if using the SIFT OVA) -
# Hayabusa (binary)                       https://github.com/Yamato-Security/hayabusa/releases
#   -> Sigma engine over EVTX (hayabusa_scan). On PATH or set $HAYABUSA_BIN.
# Chainsaw (binary)                       https://github.com/WithSecureLabs/chainsaw/releases
#   -> EVTX / Sigma triage.
# Velociraptor (binary)                   https://github.com/Velocidex/velociraptor/releases
#   -> artifact collection (vel_collect).
# YARA-X                                  bundled in the Rust crate — no separate download.

# --- toolchains (scripts/install.sh installs Rust + Python automatically) ------
# Claude Code CLI                         https://docs.anthropic.com/en/docs/claude-code/install
# Rust 1.88 (rustup)                      https://rustup.rs   (pinned in rust-toolchain.toml)
# uv (Python 3.11)                        https://docs.astral.sh/uv/getting-started/installation/
# Node 20 + pnpm                          https://nodejs.org   (apps/web dashboard)
# Docker                                  https://docs.docker.com/get-docker/  (L1 sandbox; optional n8n)

# --- evidence (never committed; pulled at run time) ---------------------------
# SANS Find Evil! starter case data       https://findevil.devpost.com/resources
# SANS HACKATHON-2026 evidence (Egnyte)   SANS-shared link (ROCBA / VANKO / SRL-2015 / SRL-2018)
# NIST CFReDS / OTRF fixtures             see docs/DATASET.md (per-fixture links + SHA-256)
